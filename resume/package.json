{
  "scripts": {
    "build": "mkdir -p dist && xelatex -output-directory=\"./dist\" resume.tex",
    "clean": "rimraf dist && mkdir -p dist",
    "watch": "chokidar \"resume.tex\" -c \"npm run build\""
  },
  "dependencies": {
    "chokidar-cli": "^1.2.0",
    "rimraf": "^2.6.1"
  }
}
